<!DOCTYPE html>
<html>
<head>
  <title>Edit Student</title>
  <link rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">

<h2>Edit Student</h2>

<form method="POST" action="/view/students/update/<%= student._id %>">
  <input name="name" value="<%= student.name %>" required class="form-control mb-2">
  <input name="roll" type="number" value="<%= student.roll %>" required class="form-control mb-2">
  <button class="btn btn-success">Update</button>
</form>

<a href="/view/students" class="btn btn-secondary mt-2">Back</a>

</body>
</html>